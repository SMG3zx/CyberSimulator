namespace CyberSimulator.OSI_Layers;

/*
 * The application layer is the only layer that directly interacts with data from the user.
 * Software applications like web browsers and email clients rely on the application layer to initiate communications.
 * It should be noted that the application layer is not the same as an application, but rather that the application layer is responsible for the protocols and data manipulation that applications rely on.
 * In so doing presenting meaningful data to the user.
 */

// Some examples of application layer protocols include:
// HTTP (HyperText Transfer Protocol) 
// SMTP (Simple Mail Transfer Protocol)

public interface ILayer7Application
{
    
}